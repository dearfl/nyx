name: Auto Rebase X11

on:
  push:
    branches:
      - main
jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Auto Rebase X11
        uses: dearfl/rebase@v1
        with:
          branch: x11
